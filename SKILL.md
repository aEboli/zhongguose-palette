---
name: zhongguose-palette
description: >
  用中国传统色配色。用户提到配色、中国色、传统色、国风配色、palette、
  色板、主色辅色点缀、故宫配色、青花、水墨、敦煌、宋瓷、唐三彩、
  江南、漆器、年画，或要给 UI / 海报 / 服装 / 空间选中国风颜色时使用本技能。
  即使只说「配个中国风的颜色」也要调用。输出必须是 zhongguose.com 526 色中的
  具名颜色，禁止发明 hex，禁止输出泛称的「中国红」。
---

# 中国传统色配色

从 `zhongguose.com` 的 526 色里，按主场 / 结构 / 点缀三个角色选出一套能看的配色。
本技能的工作不是「挑三个好看的 hex」，而是把颜色当成角色来分配，并保证它们来自色库、
分得出主次、文字能读、文化上说得通。

先跑脚本，再解释。不要凭记忆从 526 色里猜 hex——同名异色和别名陷阱太多。

## 何时读哪份文件

| 情况 | 去读 |
| --- | --- |
| 默认配色任务 | 本文件 + 跑 `scripts/palette.py` |
| 用户问「为什么这样配」「三色怎么分主次」 | `references/theory.md` |
| 用户问五行、正色间色、禁忌、色名典故 | `references/culture.md` |
| 用户点名故宫/青花/水墨等场景 | `references/curated.json` 对应条目，再跑 `--scene` |
| 用户说了库里没有的色名（石青、玄色、胭脂、黛…） | `references/aliases.json`，或 `palette.py info <色名>` |
| 对齐输出话术 | `references/examples.md` |

色库本体是 `references/colors.json`（526 条，含 LCH / 对比度 / 五行 / 角色标签）。
原始抓取件是 `references/source.json`。不要改这两个文件里的 hex。

## 铁律

1. **只出色库里的色。** 输出的每一个可见色都必须能在 `colors.json` 里按中文名找到。禁止调和出新 hex，禁止用 Tailwind / Material 的默认蓝灰粉充数。派生 token（稍深的面层、描边）必须从色库吸附最近邻，并写明来源色名。
2. **hex 是显示真源。** 8 条原始记录的 RGB 与 hex 不一致（如玫瑰灰）。一律以 hex 为准。
3. **三色是角色分工。** 主场是底、墙、纸、画布；结构是面、家具、图形块；点缀是标点（按钮、印章、金线、题名）。三者视觉分量必须依次递减，不是均分画布。
4. **按响度分角色，不按喜好。** 最静、最近中性、明度最靠两端的当主场；中等的当结构；最响、与场对比最强的当点缀。品牌主色如果是朱红，它落在点缀（或最多结构），不是铺满屏的主场。
5. **面积 ≠ 视觉重量。** 高彩点缀铺得越大，眼里越抢。脚本给出的 `pixel_pct` 才是建议铺色面积，点缀实附通常远小于名义额度。一枚朱文印能压住整张宣纸。
6. **主场必须安静。** 即使主题是朱红，场也要取汉白玉、乳白、月白、鱼肚白、粉白一类纸感色，或燕颔蓝、豆沙一类深底。禁止三枚高彩色均分画布。
7. **素底先立，彩饰后加。** 《考工记》「后素功」。留白算进主场，不是「没有颜色」。中国气质常常比通用配色更偏向大面留白，不要为了填满三色块而把点缀放大。
8. **先过对比闸门。** 正文 vs 底 ≥ 4.5:1，大字 / UI 部件 ≥ 3:1。黄、橙、浅朱在白底上经常连 3:1 都没有，不能当正文。不要把结构色原样当成字色。
9. **禁止输出「中国红」。** 落到具体色名：朱红 `#ed5126`、银朱 `#f43e06`、苋菜红 `#a61b29`、鹤顶红 `#d42517`、枫叶红 `#c21f30`。
10. **青间紫不如死。** 荆浩口诀：红间黄（秋叶）、红间绿（花簇，年画可以）、青间紫（死局）、粉笼黄（米白优于纯白）。数码蓝配洋红按青间紫处理。
11. **紫禁城红墙黄瓦是礼制特例，不是 App 模板。** 要用故宫气质：主场给石/纸，朱红降到结构，琉璃黄只作点缀线脚。
12. **金只走线，不填面。** 甘草黄、芒果黄、姜黄这类「金」用作描边、印章边、小徽章。大面积填金会立刻廉价。
13. **用户说的色名先解析。** 石青、玄色、胭脂、竹青、天青、黛、漆黑、墨色、缃色、秋香等不在 526 里。必须走别名表，告诉用户「你说的石青在本色库叫群青」，输出群青的 hex，不要用网络文学色卡里那个薄荷绿。
14. **先跑脚本再开口。** 标准入口：

```bash
python scripts/palette.py generate --scene 青花 --n 3
python scripts/palette.py generate --mood 雅 --n 5
python scripts/palette.py generate --seed 朱红 --mood 艳
python scripts/palette.py generate --dark --n 3
python scripts/palette.py generate --dark --scene 青花
python scripts/palette.py generate --media ui --scene 水墨
python scripts/palette.py generate --media poster --scene 敦煌
python scripts/palette.py generate --media fashion --scene 江南
python scripts/palette.py generate --media interior --mood 雅
python scripts/palette.py complete 月白 群青
python scripts/palette.py info 石青
python scripts/palette.py search --family 青 --role 浅底
python scripts/palette.py preview --scene 水墨 --out /tmp/preview.html
```

工作目录是本 skill 的根目录。需要 Python 3.10+，无第三方依赖。

## 工作流

1. **听意图。** 归到四类之一（可叠加）：
   - 场景：故宫 / 青花 / 水墨 / 敦煌 / 宋瓷 / 唐三彩 / 江南 / 漆器 / 年画
   - 氛围：雅 / 艳 / 古朴 / 清冷 / 浓烈 / 空灵 / 市井
   - 种子：用户给了一个色名或 hex
   - 双色：用户给了两个色，要补成一套
   媒材也要定：UI、海报/插画、服装、空间。没说就按 UI 处理，但插画不要因为对比度不够就偷换文化色。
   用户说「暗色 / 深色模式 / 夜间」时加 `--dark`：深底做场，不是把浅色方案整页反相。
   对应 CLI：`--media ui|poster|fashion|interior`。
2. **跑对应命令。** 场景优先于氛围。有种子用 `--seed`。两个色用 `complete`。
3. **读输出。** 每一套包含 60/30/10 的色名与 hex、评分、五行关系、建议像素面积、以及展开后的 token（bg / surface / text / muted / border / accent / accent_fg）。手选方案会标明出处和注意事项——那些话要原样转达给用户。
4. **交付。** 按下面的输出格式写。不要只丢三个色块；要把角色、面积、字色、不能做什么说清楚。
5. **落到用户的媒材。** UI 写成 CSS 变量；海报写成面积口令（「纸地铺满、青料勾花、深处用鷃蓝压线」）；服装写成面料角色（衣 / 缘 / 结）。

## 输出格式

```
方案 N · <评分> · <氛围/场景> · <手选方案名，若有>
主场  <色名>  <hex>  <色系/五行/彩度带>   背景、墙、纸、大面积
结构  <色名>  <hex>  ...                  卡片、家具、图形主体
点缀  <色名>  <hex>  ...                  按钮、印章、金线、题名
建议铺色面积  按脚本 pixel_pct 给出（点缀通常最小）
字色  <色名> <hex>  对比 <n>:1  AA/AAA
为何这样配：<2–4 句，含出处或荆浩/后素功/响度理由>
不要：<一条具体的禁令，如「不要把银朱铺成顶栏」>
CSS：--color-bg / --color-struct / --color-accent / --color-on-bg
```

一次给 2–3 套，说明差异（「第一套更素，第二套点缀更跳」），让用户选。
被脚本否决的方向如果用户可能想要，用一句话说明失败原因（「青间紫被荆浩口诀挡住了」），不要暗中换色。

## 角色怎么落到界面

| 角色 | UI (`--media ui`) | 海报 (`poster`) | 服装 (`fashion`) | 空间 (`interior`) |
| --- | --- | --- | --- | --- |
| 主场 | `body` 背景、主栏 | 纸地、天空、墙 | 主面料 | 墙面、地面（强制低彩） |
| 结构 | header、卡片、sidebar | 主体色块、衣物 | 缘饰、里衬 | 家具、柜门 |
| 点缀 | primary 按钮、active、badge | 印章、题名、一点朱 | 盘扣、绣、佩 | 摆件、靠垫、一扇门 |
| 字色 | 强制 ≥4.5:1，从墨色池挑 | 题名可用点缀，长文用墨 | 不做 WCAG | 标识文字用墨 |

文字和图标是 on-token，不是饼图的第四切，不要把「黑字」算进结构色。
border 用结构色的低彩近亲或场的加深，不要用点缀色描所有框。

## 氛围速查

| 氛围 | 场 | 点缀 | 允许相克 | 气质 |
| --- | --- | --- | --- | --- |
| 雅 | 浅底纸感 | 中彩，不过火 | 否 | 宋瓷、文人 |
| 艳 | 浅底或面层 | 极彩 | 是 | 宫廷、漆器 |
| 古朴 | 面层、深底 | 中深 | 否 | 土陶、青铜 |
| 清冷 | 浅底 | 冷调中彩 | 否 | 天青、月白 |
| 浓烈 | 深底 | 极彩 | 是 | 敦煌、漆器 |
| 空灵 | 几乎是纸 | 一枚印 | 否 | 水墨留白 |
| 市井 | 浅底 | 极彩，红绿可碰 | 是 | 年画、门神 |

场景会覆盖氛围：青花强制只有青与白；水墨的点缀必须是暖红印章；故宫点缀必须是朱红系（H 20°–70°），品红/紫荆红不算。

## UI token

`--media ui` 时 `tokens()` 给出 17 个可直接落地的 token，全部是库内具名色：

| 组 | token | 保证 |
| --- | --- | --- |
| 结构 | `bg` `surface` `text` `muted` `border` `border_strong` | 正文与次级都 ≥4.5:1；`border_strong` ≥3:1（输入框轮廓）；`surface` 与 `bg` ΔE ≥4 |
| 品牌 | `secondary` `accent` `accent_fg` `accent_hover` `accent_active` | 点缀上能放下 4.5:1 的字；hover/active 在 LCH 上派生后吸回色库 |
| 语义 | `success` `warning` `error` `info` | 色相锁在绿/黄/红/青区；徽章内部能放下 4.5:1 的字 |
| 状态 | `disabled` `ring` | `disabled` 低彩且与底可辨 |

三条容易踩的：

- `muted` 是次级**文字**，必须 ≥4.5:1。不要用 3:1 的大字额度糊过去。
- `warning` 这类黄色对页面底几乎永远达不到 3:1。约束在徽章内部（黄底 + 深字），不要拿黄色直接写字。
- 状态色不占三色额度。它们是第四组令牌，不要因为「accent 用完了」就把 error 改成品牌色。

不生成 Ant Design 的 10 阶交互链，也不做 Material 3 tone palette。要接这些系统，把 `accent` 当种子色交给它们自己的算法。数据可视化的分类色不在范围内——那需要等权色，与三色不等权的前提冲突。

## 可访问性底线

- 正文 4.5:1，大字 3:1，UI 部件 3:1。脚本里的 `contrast_white` / `contrast_paper` 已算好。
- 黄、橙、浅朱在纸色上对比很差，只能做装饰，不能做字。
- 色盲：点缀与场不能只靠红绿区分。脚本用 Brettel 矩阵做了 deuteranopia/protanopia 模拟，分项 `cvd` 低于 0.5 时换点缀。
- 深色模式独立推导：用 `--dark`，不要把浅色方案整页反相。深底用燕颔蓝、豆沙、苷蓝绿、钢蓝，不要用纯 `#000`；浅字用鱼肚白、粉白，不要用纯 `#fff`。
- 朱红字配深底即使对比数字够，观感也发糊，避开。

## 重建色库

数据来源：`https://zhongguose.com/colors.json`（526 色，字段 name / pinyin / hex / RGB / CMYK）。

```bash
python scripts/build_catalog.py --src references/source.json --out references/colors.json
python scripts/build_aliases.py
python scripts/selftest.py
```

`selftest.py` 覆盖：WCAG / Lab / CIEDE2000 已知值、526 条完整性、8 个别名文化校正、20 套手选方案均分、故宫点缀不得为品红。改分类或评分后必须跑通。

## 常见失败（看到就改）

- 三个颜色一样响，或最响的那个当了主场
- 输出了「中国红」或一个不在 526 里的 hex
- 把三色关系说成某个数学常数
- 青花方案里出现了红棕点缀
- 水墨方案里没有墨、点缀却是灰粉
- 故宫方案用了品红（紫荆红、丹紫红）
- 点缀色铺成了整条顶栏
- 用纯白 `#ffffff` 或纯黑 `#000000` 充场（库内最近的是雪白 / 海参灰 / 燕颔蓝）
- 金填了大面
- 正文用了姜黄、北瓜黄、银朱
